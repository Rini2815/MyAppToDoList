<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapptodolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAppToDoList">

        <!-- Splash / Launcher -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth & Main -->
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.HomeActivity" />

        <!-- Task -->
        <activity android:name=".activities.AddTaskActivity" />
        <activity android:name=".activities.DetailTaskActivity" />
        <activity android:name=".activities.EditTaskActivity" />

        <!-- Profile & Menu -->
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.PertanyaanActivity" />
        <activity android:name=".activities.TentangAplikasiActivity" />
        <activity android:name=".activities.FavoriteActivity" />

    </application>

</manifest>
